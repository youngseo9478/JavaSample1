추상클래스 = 추상메소드를 갖고있는 클래스는 추상클래스가 된다. (abstract)
인터페이스 = final static(상수)와 추상메소드로만 구성될 수있다. (interface)
	**인터페이스 네임은 보통 -able로 하는 규칙이 있다.
	**인터페이스는 설계단계에서 약속한 규약(기능)들을 정할 때 사용한다.
	**아주 강렬한 약속이 interface이고, 미세한 약속이 abstract 이다
	interface는 '틀'만 가지고 있다(호출메커니즘)
	abstract는 그냥 메소드도 있고, 추상메소드도있고 변수도 있고...
	그러면 그 약속을 바탕(인터페이스)를 타입으로 잡아서 if나 switch를 통해 
	동적바인딩을 해주면 만들고자 하는 객체에 따라 인터페이스가 갖고 있던 추상메소드를 
	각각의 오버라이딩 된 메소드로 동작시킬 수 있다.
	예) day14.lab2.Test02 참고